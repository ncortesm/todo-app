import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 20
  },
  title: {
    fontSize: 26,
    fontWeight: 'bold',
    fontVariant: ['small-caps']
  },
  count: {
    fontSize: 14,
    marginTop: 10,
    fontWeight: '500',
    color: '#666'
  }
});
